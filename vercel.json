{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bash build/ensure-wasm-build-tools.sh && corepack enable && corepack prepare pnpm@11.1.2 --activate && pnpm install --frozen-lockfile && pnpm approve-builds",
  "buildCommand": "NITRO_PRESET=vercel pnpm run build",
  "outputDirectory": ".vercel/output"
}
