{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bash build/ensure-wasm-build-tools.sh && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build"
}
